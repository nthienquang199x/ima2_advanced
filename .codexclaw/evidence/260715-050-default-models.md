# 050 default models evidence

## Scope

- GPT OAuth/API default: `gpt-5.6-luna`
- Grok image default: `grok-imagine-image-quality`
- Grok video default: `grok-imagine-video-1.5`
- UI changes affect initial/fallback selection only; persisted user selections are not migrated.
- Video extension keeps its base-model contract because extension is supported by `grok-imagine-video` only.

## Search and reuse

- Searched: `gpt-5.4-mini`, `grok-imagine-image`, `grok-imagine-video`, `normalizeGrokVideoModel`, `defaultImageModel`, `defaultVideoModel`.
- Reused `lib/imageModels.ts` normalization/constants and the existing config/env precedence. No new server abstraction or migration was added.
- Explicit-model fixtures and card-news defaults were left unchanged.

## Verification

### Baseline

`npm test` before implementation: exit 1 with 2 existing failures:

- `asset-gen-media-lightbox-contract.test.js`
- `structure-line-counts-contract.test.js`

### Static and builds

`npm run build:server && npm run build:cli && npm run typecheck && npm run typecheck:tests && cd ui && npm run build`

Tail:

```text
> ima2-gen@2.0.18 typecheck:tests
> tsc --noEmit -p tsconfig.tests.json
✓ 519 modules transformed.
✓ built in 1.14s
```

Exit: 0. Vite reported only the existing dynamic/static import and chunk-size warnings.

### Full suite

`npm test`: exit 1 with the same 2 existing failing tests and no new failing test:

- `asset-gen-media-lightbox-contract.test.js`
- `structure-line-counts-contract.test.js`

The structure failure remains pre-existing line-count drift; this task did not modify the out-of-scope structure ledger.

### Video regression

`node --test tests/videoRoute.test.ts`

```text
ℹ tests 8
ℹ suites 0
ℹ pass 8
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 532.488583
```

The added no-model POST test captures the adapter start payload and asserts `grok-imagine-video-1.5`.

### Runtime defaults and env overrides

```text
$ node -e <print image, Grok image, Grok video defaults>
gpt-5.6-luna grok-imagine-image-quality grok-imagine-video-1.5

$ IMA2_IMAGE_MODEL_DEFAULT=gpt-5.4 IMA2_GROK_VIDEO_MODEL_DEFAULT=grok-imagine-video node -e <print overrides>
gpt-5.4 grok-imagine-video
```

## Build-output note

The repository build configuration emits `config.ts` to tracked root `config.js` (`outDir: "."`). The plan's `lib/config.js` path is an untracked legacy file and is not regenerated by `npm run build:server`; it was not manually edited. All normal TS/CLI outputs were regenerated through the required build commands only.

## Hook re-verification

Fresh verification after receipt detection failed:

```text
node --test tests/videoRoute.test.ts
tests 8, pass 8, fail 0, duration_ms 464.166541

npm run typecheck && npm run typecheck:tests
typecheck + typecheck:tests: PASS
```

Judgement: the default-video regression and both static-analysis gates pass. The earlier full-suite judgement remains unchanged: only the two baseline failures are present.
