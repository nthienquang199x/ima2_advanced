{
  "provenance": {
    "provider": "runway",
    "endpoint": "https://mcp.runwayml.com/mcp",
    "fetchedAt": "2026-07-15T16:11:28.778Z",
    "serverInfo": {
      "name": "Runway",
      "title": "Runway",
      "icons": [
        {
          "src": "https://mcp.runwayml.com/icon.png",
          "mimeType": "image/png",
          "sizes": [
            "512x512"
          ]
        }
      ],
      "version": "0.1.0",
      "websiteUrl": "https://runwayml.com"
    },
    "entitlementTag": "user-oauth-account",
    "originalHash": "sha256:ad40a093773bbd794fa4ed5e5427f20f39c56ab862f9b94ec68314f13dbd81bd",
    "sanitizedHash": "sha256:bb58208e6dd3128c00b68fab85444bd1cb55e8fc7a4faf4554e90cea6bac1da0"
  },
  "serverInstructions": null,
  "tools": [
    {
      "name": "whoami",
      "title": "Account & Models",
      "description": "Returns the authenticated Runway user profile, the workspace this MCP connection is pinned to (chosen at sign-in), and the list of image/video models available in that workspace. Call this first to confirm auth and to pick a model the user can actually run. Runway account availability is checked against the currently connected workspace. If a model is unavailable in the connected workspace, the tool returns a neutral account-limitation message. If `multipleWorkspacesAvailable` is true, the user belongs to other Runway workspaces — call `list_workspaces` to see them. To switch the active workspace, the user must disconnect and reconnect the MCP from their client.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Account & Models",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "list_workspaces",
      "title": "List Workspaces",
      "description": "Lists every Runway workspace the authenticated user belongs to, with role and a compact disabled-model summary per workspace. The MCP connection is pinned to ONE workspace (chosen at sign-in); use this tool to confirm which workspace you are currently posting to and to compare options before suggesting a switch. By default this omits full disabled-model arrays to keep the response compact; pass includeDisabledModels=true only when you need the exact policy list. To switch workspaces, the user must disconnect and reconnect the Runway MCP from their client (this is intentional — it keeps account context and audit logs aligned to a single workspace per session).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "includeDisabledModels": {
            "type": "boolean",
            "description": "Include full disabledModels arrays for each workspace. Default false to avoid large responses."
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "List Workspaces",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "generate_product_marketing_video",
      "title": "Generate Product Marketing Video",
      "description": "Generate a polished creative product ad video from a product URL or product image plus a campaign idea.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nUse this for product marketing / creative ad requests like:\n- \"Make a jewelry ad featuring a chameleon in a jewelry store\"\n- \"Turn this product page into a cinematic ad\"\n- \"Use this product image and this character reference to make a funny product commercial\"\n\nv1 supports only format: `creative_ad_video`.\n\nWorkflow:\n1. Get the exact product image from `productImages[0]` or extract the best high-resolution product image from `productUrl`.\n2. Use optional `referenceImages` as character, scene, mood, or world references.\n3. Internally generate a 3x3 storyboard image, preferring GPT Image 2 for paid workspaces and falling back to Nano Banana Pro otherwise.\n4. Use that storyboard as sequential shot guidance for Seedance 2, along with the product image and references, to create the final video.\n\nPRODUCT URL FALLBACK:\n- `productUrl` extraction is best-effort. Some ecommerce sites block automated access or hide product images behind scripts.\n- If extraction fails, do not retry the same URL or claim Runway cannot make the ad. Ask the user for a screenshot of the product page or a direct product image upload, then call this tool again with `productImages` / `productImageFile`.\n- Do not call `generate_image` with a text description of the product to create the product reference. That produces a lookalike, not the real SKU.\n- For branded/luxury products, the exact product image is required for an accurate ad. Only invent or generate a stand-in product if the user explicitly agrees that an approximate substitute is acceptable.\n\nInputs:\n- productUrl: Product landing page URL. The tool will scrape metadata and rank image candidates, preferring high-resolution product/gallery images.\n- productImages: Product image URLs from uploads, recent assets, or previous generations. Preferred over productUrl extraction.\n- productImageFile: ChatGPT-only uploaded product image file. Use this when ChatGPT provides a user-uploaded product image.\n- referenceImages: Optional character/scene/mood/world references.\n- promptText: Required creative campaign idea. If the user requests a named artist/director/photographer/studio style, describe the visual qualities instead and do not include the name.\n- duration: 10 by default; use 15 only when the user asks for a longer ad.\n\nSTYLE SAFETY: Do not include names of artists, directors, photographers, studios, or other creators anywhere in prompts submitted to Runway tasks. When style direction is needed, translate it into camera language, palette, lighting, genre, era, composition, and material texture.\n\nUSER-FACING REPLIES: DO NOT mention internal model names unless asked. Do not mention the intermediate storyboard unless useful. Tell the user the creative ad video is generating and the viewer will update when ready.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "productUrl": {
            "type": "string",
            "description": "Product landing page URL. Used only if productImages is empty."
          },
          "productImages": {
            "type": "array",
            "description": "Product image URLs [{url, tag}]. Preferred over productUrl extraction.",
            "items": {
              "type": "object",
              "properties": {
                "url": {
                  "type": "string"
                },
                "tag": {
                  "type": "string"
                }
              },
              "required": [
                "url"
              ],
              "additionalProperties": false
            }
          },
          "productImageFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded product image. Prefer productImages when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. image/png."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "referenceImages": {
            "type": "array",
            "description": "Optional character, scene, mood, or world reference image URLs [{url, tag}].",
            "items": {
              "type": "object",
              "properties": {
                "url": {
                  "type": "string"
                },
                "tag": {
                  "type": "string"
                }
              },
              "required": [
                "url"
              ],
              "additionalProperties": false
            }
          },
          "promptText": {
            "type": "string",
            "description": "Required creative ad idea. If the user requests a named artist/director/photographer/studio style, describe the visual qualities instead and do not include the name."
          },
          "format": {
            "type": "string",
            "enum": [
              "creative_ad_video"
            ],
            "description": "Only supported value in v1: creative_ad_video."
          },
          "duration": {
            "type": "number",
            "enum": [
              10,
              15
            ],
            "description": "Final video duration. Default 10. Use 15 only when the user asks for longer."
          },
          "storyboardImage": {
            "type": "object",
            "description": "Internal continuation only: completed storyboard image from the first workflow stage.",
            "properties": {
              "url": {
                "type": "string"
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "productMetadata": {
            "type": "object",
            "description": "Internal continuation only: product URL metadata.",
            "properties": {
              "url": {
                "type": "string"
              },
              "title": {
                "type": "string"
              },
              "description": {
                "type": "string"
              },
              "image": {
                "type": "object",
                "description": "Backward-compatible continuation metadata from older storyboard responses. Ignored by the handler; productImages[0] is the canonical product image.",
                "properties": {
                  "url": {
                    "type": "string"
                  },
                  "width": {
                    "type": "number"
                  },
                  "height": {
                    "type": "number"
                  },
                  "alt": {
                    "type": "string"
                  },
                  "source": {
                    "type": "string"
                  },
                  "score": {
                    "type": "number"
                  }
                },
                "additionalProperties": false
              }
            },
            "additionalProperties": false
          }
        },
        "required": [
          "promptText"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Generate Product Marketing Video",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "generate_image",
      "title": "Generate Image",
      "description": "Generate OR edit an image using a Runway-hosted image model. This is the only image tool — there is no separate \"edit_image\" tool. Pass the source image as `referenceImages[0]` to edit it.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nFor creative product ad videos from a product URL or product image, use `generate_product_marketing_video` instead. It handles product-image extraction, storyboard generation, and final video creation. If product URL extraction fails, do NOT use this tool to synthesize the product from a text description unless the user explicitly agrees to a stand-in; ask for the real product image or screenshot so the ad uses the actual SKU as reference.\n\nFor video generation/editing, use `generate_video` (single shot) or `generate_multishot_video` (3-5 connected scenes).\n\nMODES:\n1. Text-to-image — pass `promptText` only. The model creates a new image from scratch. Example: \"a corgi astronaut floating in a nebula, cinematic lighting\".\n2. Image edit / transform — pass the source image as `referenceImages[0]` (give it a tag like \"input\" or \"cat\") and describe the change in `promptText`. Example: `referenceImages: [{url: ..., tag: \"cat\"}], promptText: \"@cat on a beach at sunset\"`. Use for: background swaps, style transfer, object additions/removals, restyling, photo retouching.\n3. Composite from multiple references — pass 2+ `referenceImages` each with a distinct tag, then reference them by tag in the prompt. Example: `referenceImages: [{url: ..., tag: \"person\"}, {url: ..., tag: \"room\"}], promptText: \"@person standing in @room\"`.\n\nHANDLING USER ATTACHMENTS:\nChatGPT: if it supplies an uploaded image as `referenceImageFile`, pass it directly. This tool will download the temporary ChatGPT file server-side, upload it to Runway, and use it as `referenceImages[0].url`.\nClaude/Cursor/local agents: if the user attached an image, run `init_upload` -> curl -> `complete_upload` first to get a Runway-hosted asset URL, then pass it as `referenceImages[].url`.\n\nREUSING ASSETS: URLs returned by other Runway tools (image outputs from prior `generate_image` calls, `complete_upload` outputs) are stable, hosted asset URLs. Pass them directly into `referenceImages[].url` — never re-upload.\n\nMULTIPLE IMAGES / COUNT:\nWhen the user asks for multiple images, options, variations, or directions, use `count` for the number of separate images. Rewrite `promptText` so it describes ONE standalone image only. Do NOT include phrases like \"3 images\", \"three options\", \"multiple versions\", \"grid\", \"collage\", \"contact sheet\", \"side-by-side\", or \"set of images\" in `promptText` unless the user explicitly wants a single image containing a grid/collage. For product variations, prompt for one clean standalone product composition and let `count` create separate tasks, e.g. \"single standalone product image of @product in a premium studio hero composition, not a collage or grid\".\nIf the user wants deliberately different concepts (for example studio hero, ecommerce packshot, and lifestyle scene), make separate `generate_image` calls with `count: 1` and a tailored one-image prompt for each concept.\n\nSTYLE SAFETY: Do not include names of artists, directors, photographers, studios, or other living creators as style anchors. If the user asks for a named style, translate it into neutral visual descriptors such as camera language, palette, lighting, genre, era, composition, and material texture before calling the tool.\n\nUSER-FACING REPLIES: Pick the right model internally, but DO NOT mention the model name to the user (e.g. \"using Nano Banana Pro\", \"with GPT Image 2\") unless they explicitly ask which model was used. Talk about the image content — subject, style, composition — not the engine.\n\nMODELS:\n| model            | tier   | best for                                                                                |\n|------------------|--------|[REDACTED]|\n| nano-banana-pro  | any    | DEFAULT. Photo-real images, character consistency, edits/composites with references.    |\n| gpt-image-2      | paid   | Readable text in images, charts/infographics, strict adherence to complex instructions. |\n| gen-4            | any    | Fast, low-cost general-purpose Runway model.                                            |\n\nPicking heuristic:\n- nano-banana-pro (DEFAULT) — photo-realistic images, edits, multi-image composites.\n- gpt-image-2 — pick when the image needs readable text, charts, infographics, or strict adherence to a long instruction list.\n- gen-4 — pick for speed, or when the user explicitly wants to continue with the current account settings.\n\nASPECT RATIOS:\n- nano-banana-pro: auto, 21:9, 16:9, 4:3, 1:1, 3:4, 9:16\n- gpt-image-2: 21:9, 16:9, 3:2, 4:3, 5:4, 1:1, 4:5, 3:4, 2:3, 9:16\n- gen-4: 16:9, 9:16, 1:1, 4:3, 3:4, 21:9\n\nParameters:\n- model: One of `nano-banana-pro`, `gpt-image-2`, `gen-4`. Default: `nano-banana-pro`.\n- promptText: Required. Descriptive prompt for ONE standalone output image. For edits, describe the change (\"make the background neon\"). For composites, reference tagged images (\"@cat sitting on @couch\"). If `count` > 1, remove the requested quantity from this prompt and do not ask for a grid/collage/contact sheet.\n- ratio: Aspect ratio. See per-model list above.\n- count: Number of separate image tasks to generate in one go. Options: 1, 2, 3, 4. Default: 1. This is NOT a request for one output image containing multiple panels.\n- referenceImages: Optional array of `{url, tag}` — for edits and composites. `tag` is the alias used in the prompt (e.g. `tag: \"cat\"` lets you write \"@cat\" in promptText). URLs come from prior Runway tool outputs, `complete_upload`, or any public https URL.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "model": {
            "type": "string",
            "description": "Image model id. Default: nano-banana-pro.",
            "enum": [
              "nano-banana-pro",
              "gpt-image-2",
              "gen-4"
            ]
          },
          "promptText": {
            "type": "string",
            "description": "Text prompt for ONE standalone image. If count > 1, keep the prompt about a single output, not a grid/collage/contact sheet. If the user requests a named artist/director/photographer/studio style, describe the visual qualities instead and do not include the name."
          },
          "ratio": {
            "type": "string",
            "description": "Aspect ratio. Supported values vary by model — see model description."
          },
          "count": {
            "type": "number",
            "description": "Number of separate image tasks to generate in one tool call. Options: 1, 2, 3, 4. Default: 1.",
            "enum": [
              1,
              2,
              3,
              4
            ]
          },
          "referenceImages": {
            "type": "array",
            "description": "Optional reference images [{url, tag}].",
            "items": {
              "type": "object",
              "properties": {
                "url": {
                  "type": "string"
                },
                "tag": {
                  "type": "string"
                }
              },
              "required": [
                "url"
              ],
              "additionalProperties": false
            }
          },
          "referenceImageFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded reference/source image. Prefer referenceImages when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string"
              },
              "file_id": {
                "type": "string"
              },
              "mime_type": {
                "type": "string"
              },
              "file_name": {
                "type": "string"
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "promptText"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Generate Image",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "generate_video",
      "title": "Generate Video",
      "description": "Generate OR edit a video using a Runway-hosted video model. Pass the source video as `referenceVideo` to edit/restyle it.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nCHOOSING BETWEEN THIS TOOL AND `edit_video` FOR FOOTAGE EDITS:\n- Use `edit_video` (Aleph 2.0 in-context editing) when the user wants a targeted, surgical change to existing footage and everything else must stay exactly as shot: \"make the sneakers red\", \"change the outfit\", \"remove the object on the wall\", \"change the season to winter\", \"relight the scene\", element swaps, or restyles that must preserve the original motion/framing/timing — or whenever the user mentions Aleph or Edit Studio.\n- Use THIS tool's video-to-video mode (`referenceVideo`, seedance-2) when the source video is more of a loose reference: motion transfer, style/structure inspiration, heavy re-imagining, or when the source violates Aleph's constraints (must be 2-30s, <=1080p, <=30fps, <=10 cuts, conventional aspect ratio).\n- The two are mutual fallbacks: if an `edit_video` result was unsatisfying (or the source is out of Aleph's constraints), try v2v here; if a v2v edit here changed more than the user asked for, suggest `edit_video` instead.\n\nFor creative product ad videos from a product URL or product image, use `generate_product_marketing_video` instead. It handles product-image extraction, cinematic storyboard generation, and the final ad video.\n\nFor multi-shot videos (one prompt drives 3-5 connected scenes), use `generate_multishot_video` instead.\n\nMODES:\n1. Text-to-video — pass `promptText` only. Most models support this directly. `gen-4-turbo` is the exception: for best reliability, generate a still image first with `generate_image`, then animate that image with `gen-4-turbo`. Example: \"aerial drone shot, slow push forward over misty mountains at sunrise\".\n2. Image-to-video — pass `startFrame.url` (or ChatGPT's `startImageFile`) plus `promptText` to animate a still image. Use this when you want to bring a specific image to life. Example: a product photo + \"slow 360 orbit around the bottle\".\n3. Video-to-video (edit/restyle) — pass `referenceVideo.url` plus a `promptText` describing the desired change. Use this to *modify* an existing video while preserving motion and composition: remove or replace backgrounds, remove unwanted objects/people, add new objects, swap backgrounds, change lighting, restyle scene, alter weather/time-of-day. Only `seedance-2` (DEFAULT for v2v) and `kling-o3-pro` support this mode. Example: an existing dance video + \"remove the background and place the dancer on a clean white studio backdrop\".\n\nVIDEO EDITING REQUESTS:\n- If the user asks to remove a background, remove an object/person, clean up a scene, add something to an existing video, or otherwise edit footage, DO NOT say Runway MCP cannot do it. First consider `edit_video` (Aleph 2.0) per the routing guidance above; otherwise use video-to-video here: pass the source clip as `referenceVideo` / `referenceVideoFile`, default to `seedance-2`, and describe the edit in `promptText`.\n- For background removal, ask for or use the input video as `referenceVideo` and prompt for the desired replacement/background treatment, e.g. \"remove the background and isolate the subject on a plain neutral studio backdrop\" or \"replace the background with a clean white seamless studio\".\n\nDURATION DEFAULTS (the tool sets these automatically when you OMIT `duration`):\n- Text-to-video & image-to-video → 10s (or the model's largest supported value <= 10s; veo-3.1 → 8s).\n- Video-to-video WITH `referenceVideo.durationSeconds` → largest model-supported value <= source. (e.g. 20s source with kling-o3-pro {5,10,15} → 15s; 8s source with seedance-2 {4..15} → 8s.)\n- Video-to-video WITHOUT `referenceVideo.durationSeconds` → model's MAX supported duration (15s for seedance/kling) so the edit doesn't get accidentally truncated.\n\nDURATION FOR V2V — IMPORTANT:\nAlways pass `referenceVideo.durationSeconds` when you know it (read it from the file metadata you uploaded). It's the only way the tool can know how much of the source to edit.\n- DO NOT pass `duration: 5` for a 20-second source video — that will silently clip the edit to the first 5 seconds. Just OMIT `duration` and pass `referenceVideo.durationSeconds` instead, and the tool will pick the right output length.\n- Only set `duration` explicitly when the user asks for a shorter clip (\"just make me a 5-second teaser of this 20-second video\").\n\nDO NOT use this tool to edit a still **image** → use `generate_image` and pass the image as `referenceImages[0]`.\n\nHANDLING USER ATTACHMENTS:\n- ChatGPT: if it supplies an uploaded image as `startImageFile`, pass it directly. This tool will download the temporary ChatGPT file server-side, upload it to Runway, and use it as `startFrame.url`.\n- ChatGPT: if it supplies an uploaded video as `referenceVideoFile`, pass it directly for video-to-video edits. This tool will download the temporary ChatGPT file server-side, upload it to Runway, and use it as `referenceVideo.url`.\n- ChatGPT: NEVER pass local paths such as `/mnt/data/file.png` or `/mnt/data/file.mp4` as `startFrame.url` or `referenceVideo.url`; the remote MCP server cannot read them. Use `startImageFile` / `referenceVideoFile` instead.\n- Claude/Cursor/local agents: if the user attached a local image or video, run `init_upload` -> curl -> `complete_upload` first to get a Runway-hosted asset URL, then pass it as `startFrame.url`, `endFrame.url`, `referenceImages[].url`, or `referenceVideo.url`.\n\nREUSING ASSETS: URLs returned by other Runway tools (image outputs from `generate_image`, the asset URL from `complete_upload`) are stable, hosted asset URLs. Pass them directly into the relevant field — never re-upload. Note: outputs of `generate_video` itself are NOT yet usable as `referenceVideo` (re-upload via `init_upload` if needed).\n\nUSER-FACING REPLIES: Pick the right model internally, but DO NOT mention the model name to the user (e.g. \"using Veo 3.1\", \"with Seedance 2\") unless they explicitly ask which model was used. Talk about the video content — subject, motion, mood — not the engine.\n\nMODELS (`*` = image-first workflow):\n| model        | tier | t2v | i2v | v2v | end | refs | audio | durations  |\n|--------------|------|-----|-----|-----|-----|------|-------|------------|\n| seedance-2   | paid |  Y  |  Y  |  Y  |  Y  |  Y   |   Y   | 4-15       |\n| kling-o3-pro | paid |  Y  |  Y  |  Y  |  Y  |  Y   |   Y   | 5, 10, 15  |\n| kling-3-pro  | paid |  Y  |  Y  |  -  |  Y  |  -   |   Y   | 5, 10, 15  |\n| gen-4.5      | paid |  Y  |  Y  |  -  |  -  |  -   |   Y*  | 2-10       |\n| veo-3.1      | paid |  Y  |  Y  |  -  |  Y  |  -   |   Y   | 4, 6, 8    |\n| gen-4-turbo  | any  |  *  |  Y  |  -  |  -  |  -   |   -   | 5, 10      |\n\nLegend: t2v = text-to-video, i2v = image-to-video (startFrame), v2v = video-to-video (referenceVideo, edit/restyle existing video), end = end-frame target, refs = reference images. `*` for gen-4-turbo means it is not native text-to-video: it needs a starting image. For text-only requests, create a still image first, then call gen-4-turbo with that image as `startFrame`. `*` for gen-4.5 means audio is t2v-only.\n\nPicking heuristic:\n- seedance-2 (DEFAULT for t2v/i2v/v2v) — best general-purpose, including existing-footage edits like removing/replacing backgrounds, removing objects/people, and adding new elements. Pick this unless you have a reason not to.\n- kling-o3-pro — pick when consistency matters: character/product must look identical across shots, brand or serialized content, or when v2v needs to preserve identity. Best for ads, episodic content, and product storytelling. Optimized for 1-2 main subjects.\n- kling-3-pro — pick when prompt fidelity matters more than reference-driven consistency: complex multi-character scenes (3+ people), crowded environments, experimental scripts, rapid ideation without reference assets. (No v2v support.)\n- veo-3.1 — pick when photorealism and natively-generated audio matter (Veo invents dialogue spoken by characters in-frame, plus ambient sound matched to the scene). (No v2v support.)\n- gen-4.5 — pick for keyframe-driven storytelling (provide a startFrame and the model anchors at timestamp 0). (No v2v support.)\n- gen-4-turbo — free-plan accessible image-to-video option. If the user supplied an image, pass it as `startFrame`. If the user only supplied text and a paid model is unavailable, first call `generate_image`, then call `generate_video` with `model: \"gen-4-turbo\"` and `startFrame.url` set to the generated image URL. Do not keep retrying text-only gen-4-turbo if the starter-frame step is still pending or fails. (No v2v support.)\n\nParameters:\n- model: One of `seedance-2`, `kling-o3-pro`, `kling-3-pro`, `gen-4.5`, `veo-3.1`, `gen-4-turbo`. Default for t2v/i2v: `seedance-2`. Default for v2v (when `referenceVideo` is set): `seedance-2`. Free-plan accessible option: `gen-4-turbo`, preferably with a `startFrame` from `generate_image` for text-only prompts.\n- promptText: Required. For t2v/i2v, describe the motion/style. For v2v, describe the edit (\"remove the background\", \"remove the person in the left background\", \"add a floating logo above the table\", \"make it nighttime\", \"snow falling\", \"cyberpunk neon\").\n- ratio: Aspect ratio (16:9, 9:16, 1:1, plus 4:3, 3:4, 21:9 on some models). Ignored for v2v — the model preserves the source video's ratio.\n- duration: Seconds (model-dependent — see table). USUALLY OMIT — defaults to 10s for t2v/i2v, and to the largest model-supported value <= source for v2v. See DURATION DEFAULTS above. Only set explicitly when the user asks for a non-default length.\n- startFrame: `{url}` — i2v only. Animate this image. URL must be public HTTPS or a Runway-hosted asset URL. Do NOT use ChatGPT local paths like `/mnt/data/...`; use `startImageFile` for ChatGPT uploads.\n- startImageFile: ChatGPT-only uploaded image file param `{download_url, file_id, mime_type, file_name}`. Use this instead of `startFrame` only when ChatGPT provides a user-uploaded file.\n- endFrame: `{url}` — i2v only. Final-frame target. Requires startFrame. (seedance-2, kling-o3-pro, kling-3-pro, veo-3.1.)\n- referenceImages: Array of `{url, tag}` — i2v style/subject anchors. (seedance-2, kling-o3-pro only.)\n- referenceVideo: `{url, durationSeconds?}` — v2v source video for editing/restyling existing footage, including removing/replacing backgrounds, removing objects/people, and adding elements. URL must be a Runway-hosted `/datasets/<UUID>.<ext>` URL from `complete_upload`. Do NOT use ChatGPT local paths like `/mnt/data/...`; use `referenceVideoFile` for ChatGPT uploads. Pass `durationSeconds` from your file metadata when known so the model picks an appropriate output length.\n- referenceVideoFile: ChatGPT-only uploaded video file param `{download_url, file_id, mime_type, file_name}`. Use this instead of `referenceVideo` only when ChatGPT provides a user-uploaded video.\n- generateAudio: Whether to generate native audio (model-dependent; default true for audio-capable models). For v2v with kling-o3-pro, controls whether to keep the original audio.\n- resolution: Override default resolution (seedance: 480p/720p/1080p; veo: 720p/1080p — 1080p requires 8s).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "model": {
            "type": "string",
            "description": "Video model id. Default for t2v/i2v: seedance-2. Default for v2v: seedance-2. For free-plan users, use gen-4-turbo; for text-only prompts, first create a still image with generate_image and pass it as startFrame.",
            "enum": [
              "seedance-2",
              "kling-o3-pro",
              "kling-3-pro",
              "gen-4.5",
              "veo-3.1",
              "gen-4-turbo"
            ]
          },
          "promptText": {
            "type": "string",
            "description": "Required. For text-to-video: describe the scene and camera motion. For image-to-video: describe what should happen to the image. For video-to-video: describe the edit (\"remove the background\", \"remove the person in the left background\", \"add a floating logo above the table\", \"make it nighttime\", \"swap background to a beach\")."
          },
          "ratio": {
            "type": "string",
            "description": "Aspect ratio. Supported values vary by model. Ignored when referenceVideo is provided (the model preserves the source ratio)."
          },
          "duration": {
            "type": "number",
            "description": "Duration in seconds. Supported values vary by model (see DURATION DEFAULTS in description). USUALLY OMIT THIS — when unset, the tool defaults to 10s for t2v/i2v and to the largest supported value <= source for v2v. For v2v specifically: do NOT pass `duration: 5` for a longer source video; pass `referenceVideo.durationSeconds` and leave `duration` unset."
          },
          "startFrame": {
            "type": "object",
            "description": "Image-to-video: source image to animate. URL must be public HTTPS or Runway-hosted. Do not pass ChatGPT local paths like /mnt/data/...; use startImageFile instead.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Public HTTPS or Runway-hosted image URL. Never use /mnt/data or other local file paths here."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "startImageFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded source image. Prefer startFrame.url when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. image/png."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "endFrame": {
            "type": "object",
            "description": "Image-to-video: end-frame target image. Requires startFrame. Supported by seedance-2, kling-o3-pro, kling-3-pro, veo-3.1.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Image URL."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "referenceImages": {
            "type": "array",
            "description": "Image-to-video: optional style/subject reference images. Supported by seedance-2 and kling-o3-pro only.",
            "items": {
              "type": "object",
              "properties": {
                "url": {
                  "type": "string"
                },
                "tag": {
                  "type": "string",
                  "description": "Optional alias used in the prompt (e.g. tag \"cat\" lets you write \"@cat\" in promptText)."
                }
              },
              "required": [
                "url"
              ],
              "additionalProperties": false
            }
          },
          "referenceVideo": {
            "type": "object",
            "description": "Video-to-video: source video to edit/restyle. Use this for footage edits such as removing/replacing backgrounds, removing unwanted objects or people, adding objects, and changing scene style. URL must be a Runway-hosted /datasets/<UUID>.<ext> URL returned by complete_upload. Do not pass ChatGPT local paths like /mnt/data/...; use referenceVideoFile instead. Only seedance-2 and kling-o3-pro support this; the tool will default model to seedance-2 if you set referenceVideo without specifying model.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Runway-hosted video URL from complete_upload (format: https://<cdn>/datasets/<UUID>.<ext>?_jwt=...). Never use /mnt/data or other local file paths here."
              },
              "durationSeconds": {
                "type": "number",
                "description": "Source video duration in seconds. ALWAYS pass when known — the tool uses it to auto-select the best supported output duration (largest <= source) so a 20s source produces a 15s edit instead of being clipped to the model default of 5s."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "referenceVideoFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded source video. Prefer referenceVideo.url when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. video/mp4."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "generateAudio": {
            "type": "boolean",
            "description": "Generate native audio (model-dependent). For kling-o3-pro v2v: controls whether to keep the original audio (default true)."
          },
          "resolution": {
            "type": "string",
            "description": "Output resolution (model-dependent). Seedance: 480p/720p/1080p. Veo: 720p/1080p (1080p requires 8s)."
          }
        },
        "required": [
          "promptText"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Generate Video",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "edit_video",
      "title": "Edit Video (Aleph 2.0)",
      "description": "Edit an existing video with Runway's Aleph 2.0 in-context video editor: it changes ONLY what you ask for and preserves everything else — subjects, motion, framing, timing, background, lighting.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nWHEN TO USE THIS vs generate_video:\n- USE edit_video (this tool) for targeted, surgical edits of existing footage where preservation matters: \"make the sneakers red\", \"change the outfit to a yellow fur coat\", \"remove the items on the wall behind her\", \"add graffiti on the wall\", \"change the season to winter\", \"relight the scene\", \"make it dark anime style\" — or whenever the user mentions Aleph or Edit Studio.\n- USE generate_video with referenceVideo (seedance-2) when the source video is more of a loose reference: motion transfer, using the clip as style/structure inspiration, or heavy re-imagining where preserving the original isn't the point. Also fall back to it when the source violates Aleph's constraints (see below), or when an Aleph result was unsatisfying — and suggest edit_video when a seedance v2v edit changed more than the user wanted.\n\nSOURCE VIDEO CONSTRAINTS (Aleph 2.0): 2-30 seconds, up to 1080p, up to 30fps, at most 10 cuts/shot changes, conventional aspect ratio. Cost: 28 credits/second of source (56 credit minimum), so a wasted submission is expensive — that's why the keyframe preview checkpoint below is the default.\n\nHOW THE WORKFLOW WORKS (a keyframe-driven edit is much higher quality than a text-only one):\n1. CANDIDATES — call with `promptText` + `video` only (video.durationSeconds required for this stage). The tool extracts ~4 evenly spaced frames from the video and returns them inline with their timestamps. LOOK at the frames and pick the one that most clearly shows the edit target (e.g. the widest shot for a background change; a close-up of the subject for a detail change). You may also pick your own timestamp if none fit.\n2. KEYFRAME PREVIEW — call again with the SAME arguments plus `keyframeTimestampSeconds`. The tool extracts that exact frame, edits it with an image model to apply your change, and returns the edited keyframe inline. SHOW it to the user and ask if they're happy before continuing (this is the default checkpoint). If they want a different look, re-call with a refined `promptText` or a different timestamp.\n3. SUBMIT — once approved, call again with `keyframeImage.url` set to the edited keyframe URL and the SAME `keyframeTimestampSeconds`. The tool submits the Aleph 2.0 video edit task; the result arrives via the inline viewer / `get_task`.\n\nSHORTCUTS:\n- If the user has said they want to skip the preview/approval step, pass `skipPreview: true` at step 2 — the tool generates the keyframe AND submits the video edit in one call.\n- If the user supplies their own already-edited frame, pass it directly as `keyframeImage.url` with the timestamp it came from.\n- `textOnly: true` submits a text-prompt-only edit with no keyframe — lower quality, but a valid fallback when frame extraction or the keyframe edit fails.\n\nPROMPTING (Aleph 2.0 guide): simple, targeted prompts work best — an action verb (add, remove, change, replace, re-light, re-style) plus the desired transformation, e.g. \"change the outfit and bag to soft yellow\". Because Aleph only changes what you ask for, precise language beats long descriptions. Use `extraMotionPrompt` ONLY for motion that isn't implied by the keyframe or the original video (e.g. \"fire begins to spread up the trees\") — most edits don't need it.\n\nHANDLING USER ATTACHMENTS:\n- ChatGPT: pass an uploaded video as `videoFile`; the tool imports it server-side. NEVER pass local paths like /mnt/data/... as `video.url`.\n- Claude/Cursor/local agents: upload local files first via `init_upload` -> curl -> `complete_upload`, then pass the returned asset URL as `video.url`.\n- ALWAYS pass `video.durationSeconds` when you know it (from file metadata) — the candidates stage requires it, and it makes credit accounting accurate.\n\nUSER-FACING REPLIES: Talk about the edit (what's changing in the video), not the model or task machinery. Do not mention model names unless the user asks.\n\nParameters:\n- promptText: Required. The edit instruction — what to change; everything else is preserved.\n- video: `{url, durationSeconds}` — source video. URL must be a Runway-hosted /datasets/ asset URL (from complete_upload) or a public HTTPS video URL.\n- videoFile: ChatGPT-only uploaded video file param `{download_url, file_id, mime_type, file_name}`.\n- keyframeTimestampSeconds: Timestamp (seconds) of the frame that anchors the edit. Must be within the video duration. Setting this triggers the keyframe edit + preview stage.\n- keyframeImage: `{url}` — an ALREADY-EDITED keyframe image (from step 2's preview, or user-supplied). Requires `keyframeTimestampSeconds`. Setting this submits the video edit.\n- keyframeModel: Image model for the keyframe edit. One of `nano-banana-pro`, `gpt-image-2`, `gen-4`. Default: `nano-banana-pro`.\n- extraMotionPrompt: Optional motion instruction for motion that isn't implied by the keyframe or original video.\n- skipPreview: When true at the keyframe stage, submit the video edit immediately after generating the keyframe instead of pausing for approval. Only use when the user has explicitly opted to skip the check.\n- textOnly: Submit a text-prompt-only edit with no keyframe (lower quality fallback).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "promptText": {
            "type": "string",
            "description": "Required. The edit instruction — action verb + desired change, e.g. \"make the sneakers red\", \"remove the items on the wall behind her\", \"change the season to winter\". Everything not described is preserved."
          },
          "video": {
            "type": "object",
            "description": "Source video to edit. URL must be a Runway-hosted /datasets/<UUID>.<ext> URL from complete_upload or a public HTTPS URL. Do not pass ChatGPT local paths like /mnt/data/...; use videoFile instead.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Runway-hosted dataset URL (preferred) or public HTTPS video URL. Never use /mnt/data or other local file paths here."
              },
              "durationSeconds": {
                "type": "number",
                "description": "Source video duration in seconds (2-30 supported). ALWAYS pass when known — required for the candidate-frames stage and used for accurate credit accounting."
              },
              "assetId": {
                "type": "string",
                "description": "Runway dataset asset id for the source video. Do NOT invent this. The candidate/preview stages return a resolved `video` object that includes it — pass that object back verbatim on the follow-up calls so the tool reuses the registered dataset instead of re-uploading the video."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "videoFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded source video. Prefer video.url when you already have a Runway-hosted or public URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. video/mp4."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "keyframeTimestampSeconds": {
            "type": "number",
            "description": "Timestamp (seconds) in the source video of the frame that anchors the edit. Pick it from the candidate frames this tool returns (or choose your own). Must be less than the video duration. Reuse the SAME value when later passing keyframeImage."
          },
          "keyframeImage": {
            "type": "object",
            "description": "An already-edited keyframe image — typically the preview URL returned by the keyframe stage, after user approval. Requires keyframeTimestampSeconds (the timestamp the frame was extracted at). Setting this submits the Aleph video edit.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Edited keyframe image URL (Runway-hosted or public HTTPS)."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "keyframeModel": {
            "type": "string",
            "description": "Image model used to edit the keyframe. Default: nano-banana-pro.",
            "enum": [
              "nano-banana-pro",
              "gpt-image-2",
              "gen-4"
            ]
          },
          "extraMotionPrompt": {
            "type": "string",
            "description": "Optional motion instruction for motion that is NOT implied by the keyframe or the original video (e.g. \"fire begins to spread up the trees\"). Most edits do not need this."
          },
          "skipPreview": {
            "type": "boolean",
            "description": "When true at the keyframe stage, submit the video edit immediately after generating the edited keyframe (no approval pause). Only set when the user explicitly opted to skip the preview."
          },
          "textOnly": {
            "type": "boolean",
            "description": "Submit a text-prompt-only edit with no keyframe. Lower quality — use as a fallback when the keyframe workflow is not possible."
          }
        },
        "required": [
          "promptText"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Edit Video (Aleph 2.0)",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "generate_multishot_video",
      "title": "Generate Multi-Shot Video",
      "description": "Generate a multi-shot video — 3 to 5 connected scenes from a single story or per-shot prompts. Powered by Kling 3.0 (standard at 720p, pro at 1080p). No `model` parameter — resolution selects the engine.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nUse this when the user wants a short narrative or sequence:\n- \"a 10-second mini-story about a cat finding a friend\"\n- \"a multi-scene product ad: closeup, lifestyle, hero shot\"\n- \"transition through 3 environments: forest -> desert -> beach\"\n\nFor a single continuous shot, use `generate_video` instead. For editing/restyling an existing video, also use `generate_video` (with `referenceVideo`).\n\nMODES:\n1. `auto` (DEFAULT) — Pass a single `storyPrompt` (or `promptText`, accepted as an alias) and the workflow plans the shots for you. Best when the user describes the *story* not the individual scenes. Example: `mode: \"auto\", storyPrompt: \"a barista discovers their cafe has been frozen in time\"`.\n2. `custom` — Pass per-shot prompts as `shots: [{prompt}, ...]` (3-5 shots). Total `duration` is divided evenly across them. Best when the user has specific ideas for each scene. Example: `mode: \"custom\", shots: [{prompt: \"wide shot of cafe\"}, {prompt: \"barista looks up, surprised\"}, {prompt: \"close on a frozen droplet of coffee\"}]`.\n\nOptional `firstSceneImage` anchors the opening frame for either mode.\n\nAvailability is checked against the current Runway workspace. Multi-shot tasks routinely take 5-10 minutes.\n\nUSER-FACING REPLIES: DO NOT mention the underlying engine (Kling 3.0 / Pro) to the user unless they explicitly ask which model was used. Talk about the multi-shot story — the scenes, transitions, and overall narrative — not the engine.\n\nParameters:\n- mode: `'auto'` (default) or `'custom'`.\n- storyPrompt: Required for `mode='auto'`. Full story / overall scene description.\n- shots: Required for `mode='custom'`. Array of `{prompt}`, 3-5 entries.\n- duration: Total seconds — `5` | `10` (default) | `15`. String values `'5'` | `'10'` | `'15'` are also accepted for backwards compatibility.\n- aspectRatio: `'16:9'` (default) | `'1:1'` | `'9:16'`. `ratio` is accepted as an alias.\n- resolution: `'720p'` (default, Kling 3.0 Standard) | `'1080p'` (Kling 3.0 Pro).\n- sound: Boolean, generate audio (default true).\n- firstSceneImage: Optional `{url, assetId?}` to anchor the first frame. If `assetId` is omitted, the URL is imported to a Runway dataset asset before submission because the multi-shot workflow requires a resolvable asset reference.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "mode": {
            "type": "string",
            "enum": [
              "auto",
              "custom"
            ],
            "description": "'auto' (default) or 'custom'."
          },
          "storyPrompt": {
            "type": "string",
            "description": "Required for mode='auto'. Full story or scene description. promptText is also accepted as an alias."
          },
          "promptText": {
            "type": "string",
            "description": "Alias for storyPrompt in mode='auto' for consistency with other generation tools."
          },
          "shots": {
            "type": "array",
            "description": "Required for mode='custom'. Per-shot prompts (3-5 shots).",
            "items": {
              "type": "object",
              "properties": {
                "prompt": {
                  "type": "string"
                }
              },
              "required": [
                "prompt"
              ],
              "additionalProperties": false
            },
            "minItems": 3,
            "maxItems": 5
          },
          "duration": {
            "enum": [
              "5",
              "10",
              "15",
              5,
              10,
              15
            ],
            "description": "Total duration in seconds: 5, 10 (default), or 15. String values '5', '10', and '15' are accepted for backwards compatibility."
          },
          "aspectRatio": {
            "type": "string",
            "enum": [
              "16:9",
              "1:1",
              "9:16"
            ],
            "description": "'16:9' (default), '1:1', or '9:16'. ratio is also accepted as an alias."
          },
          "ratio": {
            "type": "string",
            "enum": [
              "16:9",
              "1:1",
              "9:16"
            ],
            "description": "Alias for aspectRatio for consistency with other generation tools."
          },
          "resolution": {
            "type": "string",
            "enum": [
              "720p",
              "1080p"
            ],
            "description": "'720p' (default, Kling 3.0 Standard) or '1080p' (Kling 3.0 Pro)."
          },
          "sound": {
            "type": "boolean",
            "description": "Generate audio (default true)."
          },
          "firstSceneImage": {
            "type": "object",
            "description": "Optional first-scene anchor image.",
            "properties": {
              "url": {
                "type": "string"
              },
              "assetId": {
                "type": "string",
                "description": "Optional Runway dataset asset ID. If omitted, the MCP server imports the URL to create one before submitting."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Generate Multi-Shot Video",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "upscale_image",
      "title": "Upscale Image",
      "description": "Upscale an existing image to a higher resolution (2x, 4x, 8x, or 16x) using Runway's AI image upscaler. Use this to sharpen, denoise, and increase the resolution of an image — it does NOT change the content, composition, or style; for those edits use `generate_image` with `referenceImages`.\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nPAID PLAN REQUIRED: This upscaler is not available on free Runway workspaces. If the connected workspace is on a free plan the task will be rejected with an account-limitation message — tell the user image upscaling isn't available in their current workspace and do not retry. There is no free upscaling alternative; do NOT try to fake an upscale with `generate_image`.\n\nWHEN TO USE:\n- The user wants a low-res / soft / blurry / grainy image made crisper or larger (e.g. \"upscale this to 4x\", \"make this image higher resolution\", \"enhance / denoise this photo\").\n- The user has a finished image (their own upload, or an output from `generate_image`) and wants a higher-fidelity version of the exact same image.\n\nWHEN NOT TO USE:\n- To restyle, edit, remove/replace backgrounds or objects, or otherwise change the content → use `generate_image` with `referenceImages[0]`.\n- To generate a brand-new image → use `generate_image`.\n- To upscale a video → use `upscale_video`.\n\nINPUT:\n- Provide the source image as `image.url`: a Runway-hosted URL (from `complete_upload` or a `generate_image` output) or a public HTTPS image URL. The tool imports it into Runway automatically.\n- Pass `image.width`/`image.height` when known so scale-factor limits can be checked before submitting. The input must be at least 300x300px, and the output (width * scaleFactor x height * scaleFactor) must stay under ~25 megapixels — e.g. a 4000x3000 image only supports 1 step of 2x.\n\nFLAVOR / SCALE RULES:\n- flavor `sublime` (DEFAULT — artistic/illustrated images, smooth gradients, vibrant colors): scaleFactor 2, 4, 8, or 16.\n- flavor `photo` (photographic images, natural colors, realistic detail): scaleFactor 2 only.\n- flavor `photo_denoiser` (noise reduction for low-light/grainy photos): scaleFactor 2 only.\n\nHANDLING USER ATTACHMENTS:\n- Claude/Cursor/local agents: if the user attached a local image, run `init_upload` -> curl -> `complete_upload` first, then pass the returned URL as `image.url`.\n- ChatGPT: if it supplies an uploaded image as `imageFile`, pass it directly. NEVER pass local paths such as `/mnt/data/file.png` as `image.url`; use `imageFile` instead.\n\nUSER-FACING REPLIES: Talk about the result (\"upscaling your image 4x\"), not the underlying engine. Do not mention the model/provider unless the user explicitly asks.\n\nParameters:\n- image: `{ url, width?, height? }` — the source image to upscale. URL must be public HTTPS or Runway-hosted. Do NOT use ChatGPT local paths like `/mnt/data/...`; use `imageFile` for ChatGPT uploads.\n- imageFile: ChatGPT-only uploaded image file param `{download_url, file_id, mime_type, file_name}`.\n- scaleFactor: 2, 4, 8, or 16. Default: 2. Factors above 2 require flavor `sublime`.\n- flavor: `sublime` | `photo` | `photo_denoiser`. Default: `sublime`. Pick `photo` for photographs, `photo_denoiser` for grainy/low-light photos.\n- sharpen: 0-100. Default: 10. Higher = crisper edges.\n- smartGrain: 0-100. Default: 10. Adds natural film-like grain to avoid a plasticky look.\n- ultraDetail: 0-100. Default: 30. Higher = more invented micro-detail (can drift from the source).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "image": {
            "type": "object",
            "description": "Source image to upscale. URL must be a public HTTPS image URL or a Runway-hosted URL (complete_upload or generate_image output). Do not pass ChatGPT local paths like /mnt/data/...; use imageFile instead.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Public HTTPS or Runway-hosted image URL. Never use /mnt/data or other local file paths here."
              },
              "width": {
                "type": "number",
                "description": "Source image width in pixels. Pass when known so scale-factor limits are validated up front."
              },
              "height": {
                "type": "number",
                "description": "Source image height in pixels. Pass when known so scale-factor limits are validated up front."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "imageFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded source image. Prefer image.url when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. image/png."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "scaleFactor": {
            "type": "number",
            "description": "Upscale factor. Default: 2. Factors above 2 require flavor 'sublime'.",
            "enum": [
              2,
              4,
              8,
              16
            ]
          },
          "flavor": {
            "type": "string",
            "description": "Upscaling flavor. Default: 'sublime'. 'sublime' for artistic/illustrated images, 'photo' for photographs, 'photo_denoiser' for grainy/low-light photos.",
            "enum": [
              "sublime",
              "photo",
              "photo_denoiser"
            ]
          },
          "sharpen": {
            "type": "number",
            "description": "Sharpening strength 0-100. Default: 10."
          },
          "smartGrain": {
            "type": "number",
            "description": "Film-grain strength 0-100. Default: 10."
          },
          "ultraDetail": {
            "type": "number",
            "description": "Invented micro-detail strength 0-100. Default: 30."
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Upscale Image",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "upscale_video",
      "title": "Upscale Video",
      "description": "Upscale an existing video to a higher resolution (up to 4K) using Runway's AI video upscaler. Use this to sharpen, clean up, and increase the resolution of footage — it does NOT change the content, motion, style, or duration; for those edits use `generate_video` (video-to-video).\n\nCREDIT MODE: Runway account availability is checked against the currently connected workspace.\n\nWHEN TO USE:\n- The user wants a low-res / soft / blurry video made crisper or larger (e.g. \"upscale this to 4K\", \"make this video higher resolution\", \"enhance the quality of this clip\").\n- The user has a finished clip (their own upload, or an output from another tool that they re-uploaded) and wants a higher-fidelity version of the exact same video.\n\nWHEN NOT TO USE:\n- To restyle, edit, remove/replace backgrounds, remove objects, or otherwise change the content → use `generate_video` with `referenceVideo`.\n- To generate a brand-new video → use `generate_video`.\n\nINPUT:\n- Provide the source video as `video.url`. This must be a Runway-hosted `/datasets/<UUID>.<ext>` URL returned by `complete_upload`, OR a public HTTPS video URL (the tool imports it into Runway automatically).\n- Pass `video.durationSeconds` when known. Videos must be under 40 seconds.\n\nHANDLING USER ATTACHMENTS:\n- Claude/Cursor/local agents: if the user attached a local video, run `init_upload` -> curl -> `complete_upload` first to get a Runway-hosted asset URL, then pass it as `video.url`.\n- ChatGPT: if it supplies an uploaded video as `videoFile`, pass it directly. This tool will download the temporary ChatGPT file server-side, upload it to Runway, and use it as the source. NEVER pass local paths such as `/mnt/data/file.mp4` as `video.url`; use `videoFile` instead.\n\nREUSING ASSETS: A URL from `complete_upload` is a stable, hosted asset URL — pass it directly as `video.url`, never re-upload. Note: outputs of `generate_video` are NOT yet usable as `video.url` here (re-upload via `init_upload` if needed).\n\nUSER-FACING REPLIES: Talk about the result (\"upscaling your video to 4K\"), not the underlying engine. Do not mention the model/provider unless the user explicitly asks.\n\nParameters:\n- video: `{ url, durationSeconds? }` — the source video to upscale. URL must be public HTTPS or a Runway-hosted `/datasets` URL. Do NOT use ChatGPT local paths like `/mnt/data/...`; use `videoFile` for ChatGPT uploads.\n- videoFile: ChatGPT-only uploaded video file param `{download_url, file_id, mime_type, file_name}`. Use this instead of `video` only when ChatGPT provides a user-uploaded file.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "video": {
            "type": "object",
            "description": "Source video to upscale. URL must be a public HTTPS video URL or a Runway-hosted /datasets/<UUID>.<ext> URL returned by complete_upload. Do not pass ChatGPT local paths like /mnt/data/...; use videoFile instead.",
            "properties": {
              "url": {
                "type": "string",
                "description": "Public HTTPS or Runway-hosted video URL. Never use /mnt/data or other local file paths here."
              },
              "durationSeconds": {
                "type": "number",
                "description": "Source video duration in seconds. Pass when known — videos must be under 40s."
              }
            },
            "required": [
              "url"
            ],
            "additionalProperties": false
          },
          "videoFile": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded source video. Prefer video.url when you already have a public or Runway-hosted URL.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. video/mp4."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Upscale Video",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "init_upload",
      "title": "Start Upload",
      "description": "Initialize a file upload to Runway. Returns temporary upload URLs for direct upload.\n\nUSER-FACING REPLIES: Do not mention provider-specific storage services. Say \"Runway upload\", \"Runway-hosted asset\", or \"temporary upload URL\" instead.\n\nUSE THIS ONLY WHEN: The user has attached a NEW local file (image or video) that isn't already on Runway.\n\nCHATGPT: If ChatGPT supplies the uploaded file as `file`, pass it directly. This tool will download the temporary ChatGPT file server-side, upload it to Runway, complete the upload, and return the final asset URL. Do not ask the user to run curl.\nCHATGPT: Do NOT call this tool with only `filename`, `fileSize`, and `mimeType`; that only returns curl instructions and cannot upload bytes from ChatGPT. For image-to-video, call `generate_video` with `startImageFile`. For video-to-video, call `generate_video` with `referenceVideoFile`.\n\nDO NOT USE THIS FOR:\n- URLs returned by `generate_image`, `generate_video`, or `generate_multishot_video`. Those URLs are already stable Runway-hosted assets — pass them directly as `startFrame.url` / `referenceImages[].url` / `referenceVideo.url`. Re-uploading wastes a turn and creates a duplicate asset.\n- Any `https://` URL the user pasted that is publicly fetchable. Pass it through directly.\n\nCLAUDE/CURSOR/LOCAL WORKFLOW:\n1. Get the file size and MIME type using bash:\n   ```bash\n   wc -c < \"/absolute/path/to/your/file\" | tr -d ' '\n   file --mime-type -b \"/absolute/path/to/your/file\"\n   ```\n\n2. Call this tool with the filename, fileSize, and mimeType.\n\n3. Use bash/curl to upload the file to the temporary upload URL(s). The curl commands this tool returns include `-D -` so the response headers (including ETag) are written to stdout.\n\n4. Capture the ETag header from each PUT response (it looks like `etag: \"abc123...\"`; strip the surrounding quotes).\n\n5. Call `complete_upload` with the uploadId AND the parts array — `parts` is required even for single-part uploads.\n\n6. Use the returned assetUrl as startFrame.url / referenceImages[].url / referenceVideo.url in the generation tools.\n\nParameters:\n- file: ChatGPT-only uploaded file param `{download_url, file_id, mime_type, file_name}`. If present, the server completes the upload and returns an asset URL directly.\n- filename: Name for the uploaded file (e.g., \"video.mp4\")\n- fileSize: Size in bytes (from wc -c)\n- mimeType: MIME type (from file --mime-type). Supported: image/jpeg, image/png, image/webp, image/gif, video/mp4, video/quicktime, video/webm",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "file": {
            "type": "object",
            "description": "ChatGPT-only file param for a user-uploaded image or video. Prefer this over filename/fileSize/mimeType when ChatGPT provides it.",
            "properties": {
              "download_url": {
                "type": "string",
                "description": "Temporary ChatGPT file download URL."
              },
              "file_id": {
                "type": "string",
                "description": "ChatGPT file ID."
              },
              "mime_type": {
                "type": "string",
                "description": "MIME type, e.g. image/png or video/mp4."
              },
              "file_name": {
                "type": "string",
                "description": "Original file name."
              }
            },
            "required": [
              "download_url",
              "file_id"
            ],
            "additionalProperties": false
          },
          "filename": {
            "type": "string",
            "description": "Filename for the upload."
          },
          "fileSize": {
            "type": "number",
            "minimum": 1,
            "description": "File size in bytes. Must be greater than 0."
          },
          "mimeType": {
            "type": "string",
            "enum": [
              "image/jpeg",
              "image/png",
              "image/webp",
              "image/gif",
              "video/mp4",
              "video/quicktime",
              "video/webm"
            ],
            "description": "MIME type. Supported: image/jpeg, image/png, image/webp, image/gif, video/mp4, video/quicktime, video/webm."
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Start Upload",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "complete_upload",
      "title": "Complete Upload",
      "description": "Finalize a file upload and get the asset URL.\n\nCall this AFTER successfully uploading the file bytes to the temporary upload URL(s) from `init_upload`.\n\nUSER-FACING REPLIES: Do not mention provider-specific storage services. Say \"Runway upload\", \"Runway-hosted asset\", or \"temporary upload URL\" instead.\n\n`parts` is REQUIRED — even for single-part uploads. Each entry corresponds to one PUT response and carries the ETag from that response's `etag` header (strip surrounding quotes if present).\n\nReturns the asset URL to use as `startFrame` / `endFrame` / `referenceImages[].url` in the generation tools, or `referenceVideo.url` in `generate_video` for video-to-video edits.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "uploadId": {
            "type": "string",
            "description": "The uploadId from init_upload."
          },
          "parts": {
            "type": "array",
            "description": "Array of {partNumber, etag} from each PUT response. Required even for single-part uploads.",
            "items": {
              "type": "object",
              "properties": {
                "partNumber": {
                  "type": "number"
                },
                "etag": {
                  "type": "string"
                }
              },
              "required": [
                "partNumber",
                "etag"
              ],
              "additionalProperties": false
            },
            "minItems": 1
          }
        },
        "required": [
          "uploadId",
          "parts"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Complete Upload",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    },
    {
      "name": "list_recent",
      "title": "Recent Uploaded and Generated Assets",
      "description": "Lists recent uploaded and generated assets for the authenticated workspace. Returns asset IDs, media types, task IDs when available, and reusable asset URLs. Use this to help the user pick recent images or videos as references.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "limit": {
            "type": "number",
            "minimum": 10,
            "maximum": 50,
            "description": "Max results (10-50, default 12)."
          }
        },
        "additionalProperties": false
      },
      "annotations": {
        "title": "Recent Uploaded and Generated Assets",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "get_task",
      "title": "Check Task Status",
      "description": "Gets details for a Runway task by ID — used to check status and retrieve the result of a generation/edit task once it completes. Generation tools (`generate_image`, `generate_video`, `generate_multishot_video`) return immediately with a `task_pending` status and a task ID. If an MCP App/inline viewer was rendered for that response, do not call this tool just to poll — the original viewer handles status updates. Use this tool only when no inline viewer is available, the user explicitly asks for status, or you are recovering an older task by ID. Suggested fallback cadence: wait 30-60s for image tasks, 60-120s for video tasks, then call once. If status is still PENDING/RUNNING/THROTTLED and no inline viewer exists, wait another 30-60s and try again. When status is SUCCEEDED the response includes the asset URL.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "rationale": {
            "type": "string",
            "description": "Why are you calling this tool? What is the user actually trying to accomplish? Be specific — e.g., \"user uploaded a vacation photo and asked to animate it as a 5-second clip for Instagram\"."
          },
          "id": {
            "type": "string",
            "description": "Task ID."
          }
        },
        "required": [
          "id"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Check Task Status",
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "feedback",
      "title": "Send Feedback",
      "description": "Call this when you (the AI agent) get stuck using Runway tools.\n\nExamples of when to call:\n- A generation failed or produced unexpected results\n- You couldn't figure out which tool to use\n- The user said the result wasn't what they wanted\n- The prompting guide didn't have the info you needed\n\nThis helps the Runway team improve the tools. Be specific about what went wrong.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "One sentence: what went wrong?"
          },
          "attempted": {
            "type": "string",
            "description": "What did you try?"
          },
          "userIntent": {
            "type": "string",
            "description": "What was the user actually trying to accomplish?"
          }
        },
        "required": [
          "summary",
          "attempted",
          "userIntent"
        ],
        "additionalProperties": false
      },
      "annotations": {
        "title": "Send Feedback",
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false
      }
    }
  ]
}
